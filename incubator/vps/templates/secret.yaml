apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
data:
  login: {{ .Values.vps.user | b64enc | quote }}
  password: {{ .Values.vps.password | default (randAlphaNum 10) | b64enc | quote }}