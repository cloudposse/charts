{{- if .Values.ingress.enabled -}}

{{- $root := . -}}
{{- $serviceName := include "dashboard.mixer.fullname" . -}}
{{- $basehost := .Values.config.basehost -}}

{{ range $name, $value := .Values.upstreams }}
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ include "dashboard.mixer.fullname" $root }}-{{ $name }}-oauth
spec:
  rules:
  - host: {{ printf "%s.%s" $name $basehost  | quote }}
    http:
      paths:
      - path: /oauth2
        backend:
          serviceName: {{ template "dashboard.oauth2-proxy.fullname" . }}
          servicePort: {{ .Values.oauth2-proxy.service.http.externalPort }}

{{- if $root.Values.ingress.tls.enabled }}
  tls:
  - secretName: {{ $serviceName }}-{{ $name }}-tls
    hosts:
    - {{ printf "%s.%s" $name $basehost  | quote }}
{{- end -}}

{{- end -}}

{{- end }}
