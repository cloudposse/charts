1. As login\password always use your github credentials.
   If you use 2 Factor Authorization on github, add MFA code after password separated by space.
   For more details: https://github.com/cloudposse/github-pam
2. UI:
{{- if ( and .Values.ui.enabled .Values.ui.ssl.enabled ) }}
    NOTE: It takes time for DNS records to update and LetsEncrypt certificates to be generated.
          Please be patient. Also, we suggest reducing the TTLs on SOA to reduce cache time for non-existent DNS records.

    Use UI to get pre-generated client config
    https://{{- .Values.host -}}
{{- else if .Values.ui.enabled }}
    NOTE: It takes time for DNS records to update. Please be patient.
          Also, we suggest reducing the TTLs on SOA to reduce cache time for non-existent DNS records.

    Use UI to get pre-generated client config
    http://{{- .Values.host -}}
{{- else }}
    DISABLED

    To get openpvpn pre-generated client config run this command:
    kubectl get --namespace {{ .Release.Namespace }} secret {{ .Values.secret }} --template='{{ printf "%v%v%v" "{{" ".data.client" "}}" }}' | base64 -d > client.ovpn
{{- end }}
3. Connect to vpn with using pre-generated client config.