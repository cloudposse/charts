{{- if eq .Values.varnish.secret.name "default" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
data:
  secret: {{ .Values.varnish.secret.default | quote }}
{{ end -}}