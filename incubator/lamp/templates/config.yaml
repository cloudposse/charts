apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.app.config.name | quote }}
data:
  _k8s.settings: |-
    <?php
    $MYSQL_DB_HOST = '{{ template "mysql_host"  .}}';
  db.settings: |-
    <?php
{{ .Values.app.config.value | indent 4 }}