apiVersion: v1
kind: Secret
metadata:
  name: {{ include "common.fullname" . }}
  labels:
{{ include "common.labels.standard" . | indent 4 }}
type: Opaque
data:
  config.yaml: {{ .Values.app | b64enc }}
